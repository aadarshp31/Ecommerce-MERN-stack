import React from "react";
import Base from "../core/Base";

const AdminDashboard = () => {
	return (
		<Base title="Admin Dashboard Page" description="Welcome to Admin Dashboard Page"></Base>
	);
};

export default AdminDashboard;