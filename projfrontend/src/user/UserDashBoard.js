import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
	return (
		<Base title="User Dashboard Page" description="Welcome to User Dashboard Page"></Base>
	);
};

export default UserDashboard;