import React from "react";
import Base from "../core/Base";

const Profile = () => {
	return (
		<Base title="Profile Page" description="Welcome to Profile Page"></Base>
	);
};

export default Profile;
